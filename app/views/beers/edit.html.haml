%h1
  Edit
  = @beer.name

= form_for(@beer) do |form|
  .beer-form
    = form.label :brewery
    = form.text_field :brewery
    = form.label :name
    = form.text_field :name
    = form.label :description
    = form.text_area :description
  = form.submit "Save beer"
